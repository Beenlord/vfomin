name: Deploy to vfomin.com

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
        - name: Setup repo
          uses: actions/checkout@v4

        - name: SSH Upload  
          uses: ErenKrt/ssh-push@1.3
          with:
              host: ${{ secrets.SSH_HOST }}
              username: ${{ secrets.SSH_USER }}
              password: ${{ secrets.SSH_PASSWORD }}
              source: "./"
              destination: "/var/tmp/vfomin.zip"
              scripts: |
                mkdir /var/www/test
              # sudo rm -r /var/www/vfomin && sudo mkdir /var/www/vfomin
              # sudo chown -R www-data:www-data /var/www/vfomin
              # sudo chmod -R 755 /var/www/vfomin
              # sudo cd /var/www/vfomin &&
              # sudo mv /var/tmp/vfomin.zip . &&
              # sudo unzip vfomin.zip &&
              # sudo rm -r vfomin.zip
